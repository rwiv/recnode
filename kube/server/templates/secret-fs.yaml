---
apiVersion: secrets.hashicorp.com/v1beta1
kind: VaultAuth
metadata:
  name: {{ .Values.metadata.name }}-fs
  namespace: {{ .Values.global.namespace }}
spec:
  vaultAuthGlobalRef:
    name: default-auth
    namespace: vault
    allowDefault: true
---
apiVersion: secrets.hashicorp.com/v1beta1
kind: VaultStaticSecret
metadata:
  name: {{ .Values.metadata.name }}-fs
  namespace: {{ .Values.global.namespace }}
spec:
  type: kv-v2
  mount: {{ .Values.vault.mount }}
  path: {{ .Values.vault.fs.path }}
  version: {{ .Values.vault.fs.version }}
  destination:
    name: {{ .Values.vault.fs.secretName }}
    create: true
  refreshAfter: {{ .Values.vault.refreshAfter }}
  vaultAuthRef: {{ .Values.metadata.name }}-fs
