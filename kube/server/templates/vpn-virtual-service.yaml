{{- if .Values.vpn.enabled }}
apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: {{ .Values.vpn.name }}
spec:
  hosts:
    - {{ .Values.vpn.hostName }}
  gateways:
    - {{ .Values.gateway }}
  http:
    - route:
        - destination:
            port:
              number: {{ .Values.server.service.port }}
            host: {{ .Values.vpn.name }}
{{- end }}
