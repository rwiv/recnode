global:
  namespace: media
vault:
  mount: kube
  version: 1
  refreshAfter: 30s
secrets:
  auth:
    path: media/stdl/auth
    secretName: stdl-auth
  fs:
    path: media/stdl/fs
    secretName: stdl-fs
  queue:
    path: media/stdl/queue
    secretName: stdl-queue
