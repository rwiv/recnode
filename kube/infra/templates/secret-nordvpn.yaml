---
apiVersion: secrets.hashicorp.com/v1beta1
kind: VaultAuth
metadata:
  name: {{ .Values.global.name }}-nordvpn
  namespace: {{ .Values.global.namespace }}
spec:
  vaultAuthGlobalRef:
    name: default-auth
    namespace: vault
    allowDefault: true
---
apiVersion: secrets.hashicorp.com/v1beta1
kind: VaultStaticSecret
metadata:
  name: {{ .Values.global.name }}-nordvpn
  namespace: {{ .Values.global.namespace }}
spec:
  type: kv-v2
  mount: {{ .Values.vault.mount }}
  path: {{ .Values.vault.nordvpn.path }}
  version: {{ .Values.vault.nordvpn.version }}
  destination:
    name: {{ .Values.vault.nordvpn.secretName }}
    create: true
  refreshAfter: {{ .Values.vault.refreshAfter }}
  vaultAuthRef: {{ .Values.global.name }}-nordvpn
