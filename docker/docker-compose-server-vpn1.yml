services:
  gluetun1:
    image: qmcgaw/gluetun:v3.40
    container_name: gluetun1
    cap_add:
      - NET_ADMIN
    devices:
      - /dev/net/tun:/dev/net/tun
    ports:
      - "${SERVER_PORT_VPN1}:9083"
    volumes:
      - "${OPENVPN_CONFIG_PATH}:/ovpn/client.ovpn"
    environment:
      - VPN_SERVICE_PROVIDER=custom
      - VPN_TYPE=openvpn
      - "OPENVPN_USER=${OPENVPN_USER}"
      - "OPENVPN_PASSWORD=${OPENVPN_PASSWORD}"
      - OPENVPN_CUSTOM_CONFIG=/ovpn/client.ovpn
  stdl-vpn1:
    image: harbor.rwiv.xyz/private/stdl:0.4.8
    container_name: stdl-vpn1
    environment:
      PY_ENV: prod
      SEG_SIZE_MB: "${SEG_SIZE_MB}"
      STREAM_TIMEOUT_SEC: "${STREAM_TIMEOUT_SEC}"
      WATCHER_ENABLED: "true"

      FS_NAME: "${FS_NAME}"
      FS_CONFIG_PATH: /app/fs_conf.yaml

      OUT_DIR_PATH: /app/out
      TMP_DIR_PATH: /app/tmp

      AMQP_HOST: "${AMQP_HOST}"
      AMQP_PORT: "${AMQP_PORT}"
      AMQP_USERNAME: "${AMQP_USERNAME}"
      AMQP_PASSWORD: "${AMQP_PASSWORD}"
    entrypoint: python -m stdl server
    volumes:
      - "${FS_CONFIG_PATH}:/app/fs_conf.yaml"
      - "${OUT_DIR_PATH}:/app/out"
      - "${TMP_DIR_PATH}:/app/tmp"
    depends_on:
      gluetun1:
        condition: service_healthy
    network_mode: container:gluetun1
